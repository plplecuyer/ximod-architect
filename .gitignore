# ---- Rust / Cargo ----
# Build artifacts (regenerated by `cargo build`, can be hundreds of MB)
/target/
**/*.rs.bk
# Note: Cargo.lock IS committed for applications/binaries (kept on purpose).

# Compiled binaries accidentally copied to the project root
/ximod-architect
/ximod-architect.exe

# ---- Packaging build outputs (regenerated by the packaging scripts / CI) ----
/installer/        # Inno Setup output (setup.exe) — packaging/windows
/dist/             # macOS .app / .dmg output — packaging/macos

# ---- Local runtime config ----
Config.ini

# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
Desktop.ini
*~
*.swp
*.swo
.idea/
.vscode/
*.code-workspace
